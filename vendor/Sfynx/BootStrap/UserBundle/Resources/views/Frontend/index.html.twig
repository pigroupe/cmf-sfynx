{% set layout_nav = app.request.attributes.get('sfynx-layout') %}
{% initJquery 'TOOL:widgetimport:default' %}
{% extends layout_nav %}

{% block title %} {{ 'pi.welcome.title'|trans }} {% endblock %}

{% block content %}

    {#
    {% set is_google_analytic = getParameter('pi_app_admin.page.google_analytic') %}
    {% if is_google_analytic %}
        {% set template = getParameter('pi_app_admin.page.google_analytic_template2') %}
        {% include template %}
    {% endif %}
    #}
    
    {% set options_dashboard = {
             'action':'default',
             'menu': 'default',
             'template': 'home.html.twig',
             'locale': app.request.locale,
             'noJs': false,
           }
     %}
     {{ renderJquery('TOOL', 'dashboard', options_dashboard )|raw }}
    
{% endblock %}
