{% initJquery 'TOOL:widgetimport' %}
{% extends getParameter('sfynx.auth.theme.layout.ajax') %}

{% block global_layout %}

	{% set options_widgetimport = {
		 				'action':'widgetimport',
		 				'menu': 'widgetimport',
		 				'template': 'default.html.twig',
		 				'locale': app.request.locale,
		 				}
	%}
	{{ renderJquery('TOOL', 'widgetimport', options_widgetimport)|raw }}
	
{% endblock %}